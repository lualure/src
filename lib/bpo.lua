-- # bpo : utilities


------------------------------------------------------

require "show"
local the=require "config"
local tree=require "sdtree"
	
local function main(tr)
  lst.shuffle(tr.rows)
  for j=1,the.con.XXX
   t2 = tbl.discrete(t1)
   bins = t2.bins
